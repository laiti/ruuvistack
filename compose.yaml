# compose.yaml
services:
  influxdb2:
    image: influxdb:2
    ports:
      - 127.0.0.1:8086:8086
    environment:
      DOCKER_INFLUXDB_INIT_MODE: setup
      DOCKER_INFLUXDB_INIT_USERNAME_FILE: /run/secrets/influxdb2-admin-username
      DOCKER_INFLUXDB_INIT_PASSWORD_FILE: /run/secrets/influxdb2-admin-password 
      DOCKER_INFLUXDB_INIT_ADMIN_TOKEN_FILE: /run/secrets/influxdb2-admin-token
      DOCKER_INFLUXDB_INIT_ORG: docs 
      DOCKER_INFLUXDB_INIT_BUCKET: home
    secrets:
      - influxdb2-admin-username
      - influxdb2-admin-password
      - influxdb2-admin-token
    volumes:
      - influxdb2-data:/var/lib/influxdb2:rw
      - influxdb2-config:/etc/influxdb2:ro

  grafana:
    image: grafana/grafana-oss
    container_name: grafana
    restart: unless-stopped
    ports:
      - '127.0.0.1:3000:3000'
    volumes:
      - grafana-storage:/var/lib/grafana:rw

  mqtt5:
    image: eclipse-mosquitto
    container_name: mosquitto
    ports:
      - "127.0.0.1:1883:1883" #default mqtt port
      - "127.0.0.1:9001:9001" #default mqtt port for websockets
    volumes:
      - ./mosquitto/etc:/etc/mosquitto:rw
      - ./mosquitto/mosquitto.conf:/mosquitto/config/mosquitto.conf
    restart: unless-stopped

  ruuvibridge:
    image: ghcr.io/scrin/ruuvibridge
    container_name: ruuvibridge
    restart: unless-stopped
    # http listener (8080) and Prometheus (8081)
    ports:
      - 127.0.0.1:8080:8080
      - 127.0.0.1:8081:8081
    volumes:
      - ./ruuvibridge/config.yml:/config.yml:ro

secrets:
  influxdb2-admin-username:
    file: ~/.config/influxdb2/admin-username
  influxdb2-admin-password:
    file: ~/.config/influxdb2/admin-password
  influxdb2-admin-token:
    file: ~/.config/influxdb2/admin-token

volumes:
  influxdb2-data:
  influxdb2-config:
  grafana-storage:
  #./mosquitto/etc:
  #./mosquitto/mosquitto.conf:
  #./ruuvibridge/config.yml: